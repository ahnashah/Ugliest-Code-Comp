all_strings = ["     000000","   00      00", "  0           0"," 0             0", "0       I       0",
                            "0    changed    0", "0      the      0", "0     world     0", " 0             0","  0           0",
                            "    00      00", "      000000"]


for i in range(len(all_strings)):
    if("     000000" == all_strings[i]):
        print (all_strings[i])
 

for i in range(len(all_strings)):
    if("   00      00" == all_strings[i]):
        print (all_strings[i])
        break


for i in range(len(all_strings)):
    if("  0           0" == all_strings[i]):
        print (all_strings[i])
        break


for i in range(len(all_strings)):
    if(" 0             0" == all_strings[i]):
        print (all_strings[i])
        break

for i in range(len(all_strings)):
    if("0       I       0" == all_strings[i]):
        print (all_strings[i])


for i in range(len(all_strings)):
    if("0    changed    0" == all_strings[i]):
        print (all_strings[i])
  

for i in range(len(all_strings)):
    if("0      the      0" == all_strings[i]):
        print (all_strings[i])



for i in range(len(all_strings)):
    if("0     world     0" == all_strings[i]):
        print (all_strings[i])



for i in range(len(all_strings)):
    if(" 0             0" == all_strings[i]):
        print (all_strings[i])
        break


for i in range(len(all_strings)):
    if("  0           0" == all_strings[i]):
        print (all_strings[i])
        break

for i in range(len(all_strings)):
    if("    00      00" == all_strings[i]):
        print (all_strings[i])
        break


for i in range(len(all_strings)):
    if("      000000" == all_strings[i]):
        print (all_strings[i])